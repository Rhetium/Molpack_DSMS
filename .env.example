# Copiar a `.env` y completar. `.env` NO se versiona (ver .gitignore).

# --- PostgreSQL (requerido) ---
# DB_USER, DB_PASSWORD y DB_NAME no tienen valor por defecto:
# si faltan, la URL de conexion se arma con "None" y el arranque falla.
DB_USER=
DB_PASSWORD=
DB_NAME=
DB_HOST=localhost
DB_PORT=5432

# --- JWT ---
JWT_SECRET=
JWT_EXPIRATION_HOURS=8

# --- LDAP / Active Directory ---
# Opcionales: si se dejan vacios, la autenticacion cae al fallback JWT.
LDAP_SERVER=
LDAP_BASE_DN=
LDAP_BIND_DN=
LDAP_BIND_PASSWORD=
LDAP_GRUPO_AUTORIZADO=
LDAP_USER_FILTER=(sAMAccountName={username})
LDAP_USE_SSL=false

# --- Embeddings ---
EMBEDDING_MODEL_NAME=all-MiniLM-L6-v2
